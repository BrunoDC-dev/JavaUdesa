package Telefonica;

public class Facturador {
    public Double Factura (Registro registro,ListadeValores listadevalores, Cliente cliente, int Mes){
        registro.llamadaDe(Mes, cliente)(each)->listadevalores.tipos().filter((each).valor each.Duracion (precio)->precio.applies(each)
        return 0.0;
    }
}